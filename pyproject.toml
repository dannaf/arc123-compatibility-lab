[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "arc123-compatibility-lab"
version = "0.1.0"
description = "Evidence-gated ARC1/ARC2/ARC3 iterative hypothesis-learning research"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
