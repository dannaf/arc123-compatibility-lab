# ARC123 Status and R&D Plan

## Current evidence

| Packet | Scope | Exact post-answer result | Interpretation |
| --- | --- | ---: | --- |
| P0001 | Four curated pilot tasks | 1/4 | A generic row-span-fill theory can be learned from demonstrations and collapsed into a complete answer. |
| P0002 | Initial 10 ARC1 + 10 ARC2 filename-only curated cohort | 0/20 | The initial grammar is far too small; every task still has a complete V&V-backed NO report. |
| P0003 | Non-overlapping 10 ARC1 + 10 ARC2 transfer cohort | 0/20 | The added generic `repeat_tile` primitive passes synthetic tests but has no demonstrated transfer in this cohort. |
| P0004 | Source-pinned recorded public ARC3 `ls20` L1 transition probe | 2 observed transitions | The shared observation/action/revision contract can make, revise, and exploit a minimal external mechanics hypothesis. This is not a level-solved result. |
| P0005 | Persistent-theory controller across imported ARC12 curated 60 | 1/60 | The new controller produces complete, V&V-backed outputs for all 60. Its one exact row-span-fill result is insufficient for the multi-step curated-revision acceptance bar. |
| P0006 | P0005 plus residual-directed dihedral/blank macro tiling | 4/60 | Three additional exact tasks are solved by a generic template intersected over visible training blocks, then composed after an unresolved residual. All 56 failures remain retained. |
| P0007 | Conditional/multi-rule ARC12 revision cohort | 3/10 | The persistent theory can retain and revise a useful partial rule on three source-pinned tasks; this gates the historical P0008 measurement only. |
| P0008 | Historical frozen disjoint 25 ARC1 + 25 ARC2 cohort | 0/50 | The earlier frozen controller does not generalize on this immutable denominator. |
| P0011 | Curated-60 rectangular dihedral-coordinate development gate | 4/60 (`+0`) | The generic rectangular dihedral implementation is synthetically valid but adds no curated exact result. |
| P0012 | Curated-60 self-mask macro-stamp development gate | 8/60 (`+4`) | A generic relative-selector macro relation exactly solves four additional curated tasks; this is a development result only. |
| P0013 | Fresh filename-only frozen 25 ARC1 + 25 ARC2 cohort | 0/50 | The P0012-era controller has no exact result on a new byte-pinned, source-pinned, independently selected holdout. |
| P0014 | Disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 2/40 | A source-pinned same-cohort pre-change baseline; all 38 complete NOs and diagrams are retained. |
| P0015 | P0014 plus generic axis-mode denoising | 2/40 (`+0`) | Per-input row/column modal-support inference is synthetically valid but produces no same-cohort exact gain. |
| P0016 | Second disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 0/40 | A new source-pinned, disjoint pre-change baseline; all 40 complete NOs and diagrams are retained. |
| P0017 | P0016 plus generic self-contained-subset crop | 0/40 (`+0`) | Input-color-subset closure and unique-minimum crop are synthetically valid but produce no same-cohort exact gain. |
| P0018 | Third disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 3/40 | A source-pinned pre-component/bounding-box-portfolio baseline: 2 ARC1 and 1 ARC2 exact result; all 37 complete NOs and diagrams are retained. |
| P0019 | P0018 plus guarded frame/panel geometry | 5/40 (`+2`) | Unique largest non-solid frame-interior extraction and a visible central-divider cellwise table each add one ARC1 exact task; every 35 complete NO remains retained. |
| P0020 | Fourth disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 4/40 | A fresh source-pinned post-P0019 measurement: 3 ARC1 and 1 ARC2 exact result, with three new cross-cohort central-separator selections and all 36 complete NOs retained. |
| P0021 | P0020 plus guarded bilateral panel and dynamic scale relations | 6/40 (`+2`) | An adjacent-bilateral visible pair table adds one ARC1 exact task and unique-modal-background distinct-color scaling adds one ARC2 exact task; every 34 complete NO remains retained. |
| P0022 | Fresh 25 ARC1 + 25 ARC2 transfer cohort with P0021 vocabulary frozen | 3/50 | A source-pinned filename-only transfer measurement excluding every prior roster: two ARC1 exact results from existing relations and one ARC2 exact adjacent-bilateral pair-table result; every 47 complete NO remains retained. |
| P0023 | Fifth disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 3/40 | A source-pinned pre-change baseline after P0022: 3 ARC1 and 0 ARC2 exact results, with all 37 complete NOs retained. |
| P0024 | P0023 plus panel, stream, quadrant, and fractal portfolio | 6/40 (`+3`) | Three new generic selections—separated-panel visible tuple combination, reflective quadrant crop, and uniform-block self-stamping—add 2 ARC1 and 1 ARC2 exact task; every 34 complete NO remains retained. |
| P0025 | P0024 plus demonstration-learned hidden-zero stream background | 7/40 (`+1`) | The anti-diagonal stream now infers its shared background from visible training outputs, adding one ARC1 exact task; every 33 complete NO remains retained. This post-hoc P0024-failure correction is development evidence only. |
| P0026 | Fresh 25 ARC1 + 25 ARC2 transfer cohort with P0025 vocabulary frozen | 4/50 | A source-pinned filename-only transfer measurement excluding every prior roster: adjacent-bilateral, central-separator, and dihedral-tile selections solve 2 ARC1 plus 2 ARC2 tasks; every 46 complete NO remains retained. No P0024/P0025-new operator selected. |
| P0027 | Sixth disjoint filename-only development 20 ARC1 + 20 ARC2 baseline | 3/40 | A source-pinned pre-change baseline after P0026: 1 ARC1 and 2 ARC2 exact results, with all 37 complete NOs and diagrams retained. |
| P0028 | P0027 plus visible-evidence beam repair and bounded compatibility portfolio | 8/40 (`+5`) | Five generic selections—translation, full-palette scaling, odd-quadrant extraction, singleton border projection, and count-conditioned line generation—add 3 ARC1 and 2 ARC2 exact tasks; every 32 complete NO remains retained. |
| P0029 | Fresh all-training-split 25 ARC1 + 25 ARC2 transfer cohort with P0028 bytes frozen | 4/50 | Source-pinned, filename-only fresh transfer: 3 ARC1 and 1 ARC2 exact result, all from pre-existing rotation/dihedral-tile relations; none of P0028's new relations selects, and all 46 complete NOs remain retained. |
| P0030 | Seventh disjoint filename-only 20 ARC1 + 20 ARC2 development baseline | 2/40 | A source-pinned pre-change P0028-vocabulary baseline: 0 ARC1 and 2 ARC2 existing-relation exact results, with all 38 complete NOs and diagrams retained. |
| P0031 | P0030 plus contiguous-panel and duplicate-aware component compatibility | 4/40 (`+2`) | Conflict-free four-panel visible tuple composition adds one ARC1 exact task and duplicate-aware 4-connected crop selection adds one ARC2 exact task; all 36 complete NOs and diagrams remain retained. |
| P0032 | Fresh all-training-split 25 ARC1 + 25 ARC2 transfer cohort with P0031 bytes frozen | 4/50 | Source-pinned, filename-only fresh transfer: 2 ARC1 and 2 ARC2 exact results from pre-existing central-separator/dihedral relations; neither P0031 relation selects, and all 46 complete NOs remain retained. |
| P0033 | Eighth disjoint filename-only all-training 20 ARC1 + 20 ARC2 development baseline | 3/40 | A source-pinned pre-change P0031-vocabulary baseline: 1 ARC1 and 2 ARC2 existing-relation exact results, with all 37 complete NOs and diagrams retained. |
| P0034 | P0033 plus dynamic shared-background odd-panel crop | 4/40 (`+1`) | One ARC2 unique structural-panel crop adds one exact task while preserving P0033's three baseline YES outcomes; all 36 complete NOs and diagrams remain retained. |
| P0035 | Fresh all-training-split 25 ARC1 + 25 ARC2 transfer cohort with P0034 vocabulary frozen | 3/50 | Source-pinned, filename-only fresh transfer: 2 ARC1 and 1 ARC2 exact results from pre-existing mirror, central-separator, and dihedral relations; the P0034 odd-panel crop does not select, and all 47 complete NOs remain retained. |
| P0036 | Ninth disjoint filename-only all-training 20 ARC1 + 20 ARC2 development baseline | 2/40 | A source-pinned P0035-byte-identical baseline: 1 ARC1 and 1 ARC2 existing-relation exact results, with all 38 complete NOs and diagrams retained; the P0034 odd-panel crop does not select. |
| P0037 | P0036 plus cross-separator quadrant reflection stamp | 3/40 (`+1`) | One ARC1 structural-cross reflection/recolor selection adds one exact task while preserving P0036's two baseline YES outcomes; all 37 complete NOs and diagrams remain retained. |
| P0038 | Fresh all-training-split 25 ARC1 + 25 ARC2 transfer cohort with P0037 vocabulary frozen | 1/50 | Source-pinned, filename-only fresh transfer: the sole ARC1 exact result is pre-existing recolor; the P0037 cross-reflection primitive does not select, and all 49 complete NOs remain retained. |
| P0039 | Tenth disjoint filename-only all-training 20 ARC1 + 20 ARC2 development baseline | 3/40 | A source-pinned P0037-byte-identical baseline: three ARC1 existing-relation exact results, with all 37 complete NOs and diagrams retained; the P0037 cross-reflection primitive does not select. |
| P0040 | P0039 plus generic uniform hollow-frame size-to-color fill | 4/40 (`+1`) | One ARC1 frame-size relation adds `c0f76784`; it dynamically rederives modal-background frame slots and a conflict-free visible interior-size map, while all 36 complete NOs and diagrams remain retained. |

P0012's 60 reports; P0013's, P0022's, P0026's, P0029's, P0032's, P0035's, and P0038's 50 reports; and P0014–P0025 plus P0027/P0028/P0030/P0031/P0033/P0034/P0036/P0037/P0039/P0040's 40 reports each retain a committed full grid, explicit YES/NO all-cell validation, learning trace, receipt, and native corpus-callosum SVG; there are no abstentions or discarded failures. P0013's, P0022's, P0026's, P0029's, P0032's, P0035's, and P0038's fresh rosters and P0014/P0016/P0018/P0020/P0023/P0027/P0030/P0033/P0036/P0039's disjoint development rosters were selected before any selected JSON was decoded, using only source-pinned filenames, split labels, fixed SHA-256 ranking, and opaque source-byte checksums.

## What has been validated

- The live controller uses only training input/output evidence plus a test input. Historical ARC12 schemas, GT feature contracts, task IDs, GT solver code, decompositions, and test targets are outside its answer-selection interface.
- A static ARC task can operate as an `ARC12InteractiveEnv`: demonstrations are parallel evidence worlds and attention/propose/compare/counterexample/commit are observable internal actions.
- Compatibility preserves `UNKNOWN != IMPOSSIBLE`; observed contradiction receives exact zero support while incomplete partial theories remain explicit rather than being treated as failure.
- The default learner now revises a first-class `PartialTheory`: a counterexample can change only the offending rule's scope, retain its explanatory region, add a residual rule, and compose the result. Synthetic tests exercise that causal sequence and dynamic demo selection.
- P0006 records four exact curated outcomes under the persistent controller. Its three dihedral-tile results follow `identity partial theory → UNKNOWN residual → generic structural proposal → ordered composition → exact train theory → test commit`; the row-span result also retains explicit counterexamples.
- P0012 adds a generic self-mask macro-stamp relation. It infers a relative most/least-frequency, zero, or nonzero selector and either an input or selector-mask template exclusively from visible demonstrations; it produces four additional exact curated results without task IDs or test-target access.
- P0013 validates the stronger measurement boundary itself: a clean detached source revision, frozen controller/evaluator bytes, source-task SHA-256 values, fixed 25+25 roster, source-pinned `--verify`, and visually reviewed corpus-callosum diagrams. Its score is nevertheless `0/50`.
- P0014 fixes a disjoint 20+20 development denominator before its baseline controller is run: `2/40` exact, with a complete committed answer, receipt, trace, and visual diagram for every task. Its report set reproduces byte-for-byte.
- P0015 adds a generic `axis_mode_denoise` primitive. It dynamically recomputes row/column unique modes and modal support per grid, refuses all ties, and is proven by synthetic mixed-orientation examples. An offline-only source-pinned GT feature vocabulary motivated the family but is neither imported nor available to the live controller. The P0015 comparison is `2/40` versus P0014's `2/40` (`+0`), so it has no task-independent transfer evidence.
- P0016 freezes a second metadata-only development cohort that excludes P0012, P0013, and P0014/P0015 task IDs. Its baseline is `0/40`; every task has a source-pinned full-grid post-answer V&V record and a visually reviewed corpus-callosum diagram.
- P0017 adds a generic `self_contained_subset_crop` primitive. It enumerates only colors visible in the current input, finds subset-induced rectangles closed under their own colors, and returns a crop only when the smallest non-full-grid candidate is unique. A source-pinned offline-only GT feature vocabulary motivated it, but the live controller imports none of that material. P0017 remains `0/40` versus P0016's `0/40` (`+0`), so it also has no task-independent transfer evidence.
- P0018 freezes a third metadata-only development cohort that excludes every prior imported, holdout, and development roster. Its P0017-vocabulary baseline is `3/40` (`2` ARC1 and `1` ARC2), source-pinned and clean-process reproducible; all 40 reports were retained and a varied solved/failed SVG sample was visually checked for legibility.
- P0019 adds two generic geometry hypotheses after P0018 was frozen: `frame_interior_crop` requires a unique largest non-solid uniform rectangular outline and independently rederives it for each grid; `central_separator_cellwise_combine` requires equal panels at a uniform central divider, learns only visible aligned color pairs, and leaves an unseen pair unpredicted. Both have synthetic success and ambiguity/unseen-pair refusal contracts. P0019 is clean-process source-pinned reproducible at `5/40` versus P0018's `3/40` (`+2`); the two new exact results each use one new generic relation and no live GT or task identifier input. A solved/failed SVG contact sheet was visually checked for legibility.
- P0020 freezes a fourth metadata-only cohort excluding all prior roster IDs after the P0019 implementation checkpoint and before its task grids were parsed. Its clean-process source-pinned baseline is `4/40` (`3` ARC1 and `1` ARC2), including three exact `central_separator_cellwise_combine` selections—two ARC1 and one ARC2—with independently learned horizontal lookup tables. This is limited cross-cohort development replication of that guarded family, not a fresh benchmark-wide holdout or solver claim. All 40 reports were retained and a five-task solved/failed SVG sample was visually reviewed.
- P0021 adds two guarded input-derived relations after P0020 was frozen: `adjacent_bilateral_cellwise_combine` requires exactly two adjacent equal panels, a single conflict-free orientation, and an aligned finite table learned only from visible training pairs; `distinct_nonbackground_scale` requires a uniquely inferred modal background and recomputes its integer scale from the current grid's distinct non-background colors. Synthetic tests prove dynamic rederivation plus unseen-pair and tied-background refusal. P0021 is clean-process source-pinned reproducible at `6/40` versus P0020's `4/40` (`+2`), with the two new exact results selected by those two new generic relations and no task ID, GT, cohort metadata, or held-out target passed live. All 40 reports are retained, and a solved/failed eight-diagram SVG contact sheet was visually reviewed for legibility.
- P0022 freezes a new 25+25 filename-only transfer cohort after the P0021 controller checkpoint. It excludes every ID in the historical import, P0013 frozen roster, and P0014/P0016/P0018/P0020 development rosters before any selected P0022 task payload is decoded. Its clean-process source-pinned result is `3/50` (`2` ARC1 and `1` ARC2); the ARC2 exact result is a new `adjacent_bilateral_cellwise_combine(axis=horizontal, ...)` selection, giving that guarded family one independent task-level transfer result. The other two exact selections use existing top-bottom and self-contained-subset relations; `distinct_nonbackground_scale` has no P0022 exact selection. All 50 reports, including 47 complete NOs, are retained, and an eight-diagram solved/failed SVG contact sheet was visually reviewed. This validates a reusable primitive under the promotion rule, not broad task solving or an ARC solver.
- P0023 freezes a fifth metadata-only development cohort after P0022 and before its selected task payloads are decoded. It excludes every historical, P0013/P0022 frozen-transfer, and P0014/P0016/P0018/P0020 development task ID. Its clean-process source-pinned P0021-vocabulary baseline is `3/40` (`3` ARC1 and `0` ARC2); all 40 reports are retained and a varied solved/failed eight-diagram sample was visually reviewed. It is a fixed same-cohort denominator for the next portfolio, not a transfer or solver result.
- P0024 adds four bounded generic relations after P0023's pre-change baseline: regularly separated multi-panel visible-tuple combination, anti-diagonal stream expansion, reflective foreground-quadrant crop, and uniform-block self-stamping. Synthetic tests cover dynamic rederivation and ambiguity/refusal conditions. Its clean-process source-pinned same-cohort result is `6/40` versus P0023's `3/40` (`+3`): two new ARC1 selections and one new ARC2 selection. All 40 reports are retained and solved/failed diagrams were visually reviewed. This is development evidence, not transfer.
- P0025 corrects only the stream relation's background inference: it binds a unanimous unique-modal background from the visible training outputs rather than discarding an otherwise fitting relation when that background ties in a training input. It is clean-process source-pinned reproducible at `7/40` versus P0024's `6/40` (`+1`), retaining every earlier P0024 solve and adding ARC1 `feca6190`. The P0024 post-answer failure informed the correction, which is explicitly recorded in its packet; P0025 is post-hoc development evidence and cannot support a transfer or solver claim. All 40 task reports and diagrams are retained.
- P0026 freezes a new 25+25 filename-only transfer cohort after P0025, excluding every historical, P0013/P0022 transfer, and P0014/P0016/P0018/P0020/P0023 development task ID before any selected P0026 payload is decoded. Its clean-process source-pinned result is `4/50` (`2` ARC1 and `2` ARC2); exact selections reuse `adjacent_bilateral_cellwise_combine`, `central_separator_cellwise_combine`, and residual-directed `dihedral_tile`. No P0024/P0025-new relation selects, which is retained as negative transfer evidence. All 50 reports—including 46 complete NOs—are retained, and a solved/failed SVG contact sheet plus an independent fallback diagram were visually reviewed. This is limited whole-vocabulary transfer evidence, not broad task solving or an ARC solver claim.
- P0027 freezes a sixth metadata-only development cohort after P0026 and before its selected task payloads are decoded. Its clean-process source-pinned P0025-vocabulary baseline is `3/40` (`1` ARC1 and `2` ARC2); all 40 reports are retained and the baseline cannot be retuned. It is the fixed same-cohort denominator for the next generic portfolio, not transfer or a solver result.
- P0028 freezes a visible-training compatibility ranking before beam pruning and five bounded generic relations after P0027's immutable baseline. Exact translation offsets now survive initial search only when visible demonstrations uniquely fix the offset; the new full-palette scale, central-cross odd-quadrant, singleton-seed border, and color-count line relations each dynamically rederive their visible inputs and refuse ambiguity. Its clean-process source-pinned same-cohort result is `8/40` versus P0027's `3/40` (`+5`; `4` ARC1, `4` ARC2), retaining all 40 reports. A ten-diagram solved/failed contact sheet was visually reviewed for layout, output geometry, and readable evidence flow; the stored SVG text is valid Unicode, while this Mac's ImageMagick fallback lacks a Unicode glyph delegate. This is development evidence only, not transfer or a solver claim.
- P0029 freezes P0028's controller bytes on a new source-pinned filename-only `25 ARC1 + 25 ARC2` cohort before any selected grid is parsed. It uses all-training source splits because only ten unseen ARC2 evaluation filenames remain after the prior disjoint rosters; that constraint is explicit and prevents a false claim of the former mixed-split protocol. Its clean-process source-pinned `--verify` reproduces all 202 report artifacts byte-for-byte: `4/50` exact (`3/25` ARC1, `1/25` ARC2), `46` complete wrong predictions, `4` training-exact theories, and `46` complete fallbacks. The four YES selections are existing `rotate_180`/`dihedral_tile` relations; no P0028-new relation selects, so the P0028 development gain has no independent task-level reuse in this transfer cohort. Native SVG/XML, all receipt hashes, 50 full grids, and representative solved/failure diagrams were checked; ImageMagick has a local Unicode font-delegate limitation, but its correctly sized rasters show intact input/core/output layout and the native SVG text preserves the intended Unicode labels. This is limited all-training-subset transfer evidence, not a solver claim.
- P0030 freezes the unchanged P0028 controller before any selected seventh-development-cohort task is parsed. Its clean-process source-pinned `--verify` reproduces all 162 report artifacts byte-for-byte: `2/40` exact (`0/20` ARC1, `2/20` ARC2), `38` complete wrong predictions, `2` training-exact theories, and `38` complete fallbacks. The YES selections are existing `dihedral_tile` and `central_separator_cellwise_combine` relations, so it is a fixed same-cohort denominator rather than a new capability claim. All receipt hashes, 40 complete grids, native SVG/XML diagrams, and representative solved/failure diagrams were validated; the local raster caveat is identical to P0028/P0029, while native SVG labels remain correct. P0030 is development evidence only, not transfer or a solver claim.
- P0031 freezes the `fd3ac79b5415d0f7b42747c5bff19829802ccde3` controller after P0030's immutable development baseline. `contiguous_panel_cellwise_combine` infers one visible, conflict-free tuple table across at least three equal contiguous panels and refuses unknown tuples or axis ambiguity; `unique_component_crop` rederives a unique modal background, color-sensitive 4-connected regions, and an exactly-once crop against exactly-twice peers, refusing ties and diagonal-only contact. Its clean-source reproduction covers all 162 artifacts byte-for-byte: `4/40` exact (`1/20` ARC1, `3/20` ARC2), `36` complete wrong predictions, `4` training-exact theories, and `36` complete fallbacks. The two newly selected exact tasks are ARC1 `3d31c5b3` and ARC2 `cd3c21df`; all receipt hashes, full grids, native SVG/XML diagrams, and representative solved/failure layouts were checked. ImageMagick again lacks the local Unicode glyph delegate, but native SVG retains `UNKNOWN ≠ IMPOSSIBLE` and the rasterized geometry is intact. P0031 is same-cohort development evidence only, not transfer or a solver claim.
- P0032 freezes the P0031 `fd3ac79b5415d0f7b42747c5bff19829802ccde3` controller on a new source-pinned filename-only all-training `25 ARC1 + 25 ARC2` cohort before any selected task payload is parsed. Its clean-process `--verify` reproduces all 202 artifacts byte-for-byte: `4/50` exact (`2/25` ARC1, `2/25` ARC2), `46` complete wrong predictions, `4` training-exact theories, and `46` complete fallbacks. The YES selections are pre-existing `central_separator_cellwise_combine`, `dihedral_transform`, and `dihedral_tile`; neither `contiguous_panel_cellwise_combine` nor `unique_component_crop` selects. All receipt hashes, 50 full grids, native SVG/XML diagrams, and representative solved/failure layouts were checked. ImageMagick retains the same local Unicode glyph-delegate limitation, while native SVG text and complete geometry remain correct. This is negative all-training-subset transfer evidence for the two P0031 relations, not a solver claim.
- P0033 freezes the unchanged P0031 generic vocabulary at `5de7928e61ab625be59abab70ce3570e018cbd2e` after its filename-only all-training 20+20 roster is committed and before any selected grid is parsed. Its clean-process source-pinned `--verify` reproduces all 162 artifacts byte-for-byte: `3/40` exact (`1/20` ARC1, `2/20` ARC2), `37` complete wrong predictions, `4` training-exact theories, and `36` complete fallbacks. The YES selections are pre-existing `dihedral_tile`, `recolor`, and `central_separator_cellwise_combine`; neither P0031 relation selects. All receipt hashes, 40 full grids, native SVG/XML diagrams, and representative ARC1/ARC2 YES and NO layouts were checked. Native macOS rendering shows uncropped input/core/output geometry and readable evidence flow. P0033 is an exposed development baseline only, not transfer or a solver claim.
- P0034 freezes the `3e04e03086528da6a9a2107a08ebdb4f219bdae4` controller after P0033's immutable baseline and pre-registers a one-family same-cohort comparison. `repeated_panel_odd_one_out_crop` dynamically rederives a shared panel background, panel orientation, and one occupancy-mask outlier; mixed-orientation and changing-background synthetic tests prove its scope, while tied masks and nonunique shared backgrounds refuse. Its clean-process source-pinned `--verify` reproduces all 162 artifacts byte-for-byte: `4/40` exact (`1/20` ARC1, `3/20` ARC2), `36` complete wrong predictions, `5` training-exact theories, and `35` complete fallbacks. It preserves all P0033 YES outcomes and adds only ARC2 `a87f7484`. All receipt hashes, full grids, and 40 native SVG/XML diagrams were checked. Native visual review exposed an avoidable low-contrast `0`/`6` palette collision in the historic P0034 renderer; the immutable report root remains unchanged, while commit `e869c48` adds a tested magenta rendering for later diagrams and a corrected external re-render confirms the new witness's panel structure is legible. P0034 is same-cohort development evidence only, not transfer or a solver claim.
- P0035 freezes the P0034 vocabulary on a newly source-pinned filename-only all-training 25+25 cohort before any selected task payload is parsed. Its renderer changes only the `0`/`6` palette contrast; all answer-selection source bytes and generic operator vocabulary match P0034. Its clean-process `--verify` reproduces all 202 artifacts byte-for-byte: `3/50` exact (`2/25` ARC1, `1/25` ARC2), `47` complete wrong predictions, `3` training-exact theories, and `47` complete fallbacks. The YES selections are pre-existing `left_right`, `central_separator_cellwise_combine`, and `dihedral_tile`; `repeated_panel_odd_one_out_crop` has no selection. All receipt hashes, 50 full grids, native SVG/XML diagrams, and representative ARC1/ARC2 YES and NO layouts were checked with the corrected palette. This is negative all-training-subset transfer evidence for the P0034 relation, not a solver claim.
- P0036 freezes the same `e869c4842817624925e6576a1be9bb1f27399977` P0035 controller bytes after its ninth disjoint filename-only all-training 20+20 roster is committed and before any selected grid is parsed. Its clean-process source-pinned `--verify` reproduces all 162 artifacts byte-for-byte: `2/40` exact (`1/20` ARC1, `1/20` ARC2), `38` complete wrong predictions, `2` training-exact theories, and `38` complete fallbacks. The YES selections are pre-existing `quadrant_odd_one_out` and `central_separator_cellwise_combine`; `repeated_panel_odd_one_out_crop` has no selection. All receipt hashes, 40 full grids, and native SVG/XML diagrams were checked; native macOS rendering visually reviewed an ARC1 YES, ARC2 YES, and magenta-bearing ARC2 NO. The immutable P0036 diagram for the long central-separator name visibly truncates that label, so the next renderer revision must make labels fully readable without rewriting frozen evidence. P0036 is a development baseline only, not transfer or a solver claim.
- P0037 freezes `5593527a16a57bcc0925ae3692b0888f141452e3` after P0036's immutable baseline and preregisters one same-cohort comparison. `cross_separator_quadrant_reflection_stamp` dynamically requires one centered uniform cross, exactly one nonuniform payload quadrant, three equal uniform background quadrants, and a single payload color; it reflects the payload mask across both axes and recolors it to the structural-cross color, refusing multiple payload quadrants and a background-colored separator. Its color/orientation-varying synthetic contract and actual P0036 scope audit find one training fit. Its clean-process source-pinned `--verify` reproduces all 162 artifacts byte-for-byte: `3/40` exact (`2/20` ARC1, `1/20` ARC2), `37` complete wrong predictions, `3` training-exact theories, and `37` complete fallbacks. It preserves P0036's two exact results and adds only ARC1 `47c1f68c`. All receipt hashes, full grids, and 40 native SVG/XML diagrams were checked; native macOS renders verify the new cross-reflection YES, readable central-separator label, and a magenta-bearing ARC2 NO. P0037 is same-cohort development evidence only, not transfer or a solver claim.
- P0038 freezes the same `5593527a16a57bcc0925ae3692b0888f141452e3` P0037 controller on a new source-pinned filename-only all-training 25+25 cohort before any selected task payload is parsed. Its clean-process source-pinned `--verify` reproduces all 202 artifacts byte-for-byte: `1/50` exact (`1/25` ARC1, `0/25` ARC2), `49` complete wrong predictions, `1` training-exact theory, and `49` complete fallbacks. The one YES is pre-existing `recolor`; `cross_separator_quadrant_reflection_stamp` has no selection. All receipt hashes, 50 full grids, native SVG/XML diagrams, and native macOS rendered YES/NO layouts were checked with the distinct magenta palette. This is negative all-training-subset transfer evidence for the P0037 primitive; it does not meet the promotion rule and is not a solver claim.
- P0039 freezes the same `5593527a16a57bcc0925ae3692b0888f141452e3` P0037 controller after its tenth disjoint filename-only all-training 20+20 roster is committed and before any selected grid is parsed. Its clean-process source-pinned `--verify` reproduces all 162 artifacts byte-for-byte: `3/40` exact (`3/20` ARC1, `0/20` ARC2), `37` complete wrong predictions, `3` training-exact theories, and `37` complete fallbacks. The YES selections are pre-existing `dihedral_transform`, `adjacent_bilateral_cellwise_combine`, and `distinct_nonbackground_scale`; `cross_separator_quadrant_reflection_stamp` has no selection. All receipt hashes, 40 full grids, and native SVG/XML diagrams were checked; native macOS rendering visibly shows the three YES geometries and a magenta-bearing NO. The immutable P0039 diagrams reveal that long adjacent-bilateral and nonbackground-scale labels still truncate, so the next renderer revision must use concise readable labels without rewriting frozen evidence. P0039 is a development baseline only, not transfer or a solver claim.
- P0040 freezes `c3842b7d28fb2829b79ec815a99b1bc39912a189` after P0039's immutable same-cohort baseline and preregisters one bounded generic relation. `uniform_frame_size_fill` dynamically rederives a unique modal background, all 4-connected monochrome hollow rectangular perimeters, and a conflict-free visible interior-height/interior-width-to-fill-color table. It refuses tied backgrounds, contaminated hidden-background slots, fewer than two frames or learned interior sizes, conflicting evidence, and unseen test sizes. Its synthetic contract varies backgrounds, frame colors, locations, dimensions, fill colors, and unrelated components. The clean-process source-pinned `--verify` reproduces all 162 P0040 artifacts byte-for-byte: `4/40` exact (`4/20` ARC1, `0/20` ARC2), `36` complete wrong predictions, `4` training-exact theories, and `36` complete fallbacks. It preserves the three P0039 YES outcomes and adds only ARC1 `c0f76784`. XML parses all 40 diagrams; native macOS renders show a readable frame-size YES, a readable bilateral YES, and a distinct-magenta retained NO. P0040 is same-cohort development evidence only, not transfer or a solver claim.
- P0041 freezes the P0040 live answer-selection bytes on a newly committed source-pinned filename-only final all-training `19 ARC1 + 19 ARC2` cohort before any selected task grid is parsed. The smaller denominator is a declared capacity boundary: after all historical/development/transfer roster imports, no ARC2 evaluation filenames and only 19 ARC1 training filenames remained; P0041 consumes those final disjoint ARC1-training slots rather than pretending a 25+25 cohort exists. Its clean-source `--verify` reproduces all 154 report artifacts byte-for-byte: `1/38` exact (`1/19` ARC1, `0/19` ARC2), `37` complete wrong predictions, one training-exact theory, and 37 complete fallbacks. The sole YES, ARC1 `94f9d214`, is pre-existing `adjacent_bilateral_cellwise_combine`; `uniform_frame_size_fill` selects zero times. XML parses all 38 diagrams; native macOS rendering visibly checks the bilateral YES, ARC1/ARC2 NO layouts, and a distinct-magenta retained NO. P0041 is final limited negative all-training-subset transfer evidence for frame-size fill, not a solver claim.
- P0042 freezes the same P0040 live answer-selection bytes on a newly committed source-pinned filename-only mixed-partition `20 ARC1 evaluation + 20 ARC2 training` cohort before any selected task grid is parsed. This is a forced protocol change after P0041 exhausted the declared ARC1 all-training capacity; it is explicitly non-comparable to all-training transfer and cannot be used for benchmark-submission claims. Its clean-source `--verify` reproduces all 162 report artifacts byte-for-byte: `2/40` exact (`0/20` ARC1, `2/20` ARC2), `38` complete wrong predictions, two training-exact theories, and 38 complete fallbacks. Both YES outcomes—ARC2 `cf5fd0ad` and `59341089`—use pre-existing dihedral macro-tile compositions; no P0040 frame-size selection occurs. XML parses all 40 diagrams; native macOS rendering visibly checks both dihedral YES diagrams, ARC1/ARC2 NO layouts, and a distinct-magenta retained NO. P0042 is an immutable exposed baseline for one future same-cohort relation, not transfer or a solver claim.
- ARC12 and ARC3 adapters use shared neutral contracts. The ARC3 adapter reads only an allowed, source-pinned public segmented trajectory, hides source/future metadata from the agent view, and refuses unmatched actions rather than simulating a result.
- The offline-only `ARC12-IHL-GT` pilot and `ARC3-IHL-GT` audit inventory are materialized and schema-validated. Tests ensure live adapters do not import those oracle-lane readers and reject oracle/final-rule paths.
- Brain-surgery reports are deterministic artifacts. The packet verifier reproduces the complete output directory, not merely the top-level score receipt.

## What is not validated

- No ARC-AGI-1 or ARC-AGI-2 solver claim is justified. P0012 reaches only `8/60` on a development cohort, P0015 is unchanged at `2/40` on a separate development cohort, P0017 is unchanged at `0/40` on another disjoint development cohort, P0019 reaches `5/40`, P0021 reaches `6/40`, P0024/P0025 reach `6/40` then `7/40`, P0028 reaches `8/40`, and P0031/P0034/P0037/P0040 reach `3`–`4/40` only after same-cohort development; P0020, P0023, P0027, P0030, P0033, P0036, and P0039 each reach only `2`–`4/40` on fresh but still development cohorts, P0013 is `0/50`, P0022 is `3/50`, P0026 is `4/50`, P0029 is `4/50`, P0032 is `4/50`, P0035 is `3/50`, P0038 is `1/50`, and P0041 is `1/38` on fresh but explicitly all-training-split cohorts. None supports a benchmark-wide solver claim.
- P0042's `2/40` is an internal mixed-partition exposed baseline, not a transfer result or a benchmark score. Its two pre-existing dihedral witnesses do not offset P0041's final negative frame-size transfer evidence or establish broad generalization.
- P0012 was designed and measured on the same curated development roster, so its `+4` is not independent transfer evidence. P0013's `0/50` and P0022's `3/50` use different frozen vocabularies and cohorts, so they are not a direct score delta; together they show that the architecture remains far from broad generalization.
- Issue #2's architecture mechanism is supported by synthetic revision tests and a small set of retained, failure-inclusive traces. Its final acceptance/closure still requires broad, independently replicated transfer; P0013 rules out closing it on the current operator vocabulary.
- P0004 validates a real recorded transition stream and shared contracts, not blind live ARC3 play or a solved ARC3 level.
- The generic grammar does not yet cover object selection, broad component/bounding-box extraction, region filling, structured repetition, general panel relations, or multi-step composition at the required breadth.
- P0002/P0003 are curated-development evidence, not the frozen 25+25 generalization denominator.

## Next gated work

1. Preserve P0013, P0022, P0026, P0029, P0032, P0035, P0038, and P0041 as immutable transfer evidence. They may inform an offline failure taxonomy, but no selected transfer task may alter its frozen controller or be represented as an improvement on that packet.
2. Preserve P0023, P0024, P0025, P0027, P0028, P0030, P0031, P0033, P0034, P0036, P0037, P0039, P0040, and P0042 as exposed development evidence. Their denominators, source pins, full-grid commitments, and task artifacts remain immutable. P0042 is an explicitly non-comparable mixed-partition baseline, not transfer.
3. Keep P0038's `1/50` and P0041's `1/38` transfer results plus P0039's `3/40`, P0040's `4/40`, and P0042's `2/40` baselines immutable. P0038 has zero cross-reflection selections and P0041 has zero frame-size-fill selections; P0042's two dihedral solves are pre-existing vocabulary, so no primitive meets the promotion rule.
4. The future-only renderer repair is complete; do not rewrite P0036 or P0039 frozen artifacts. P0041 exhausts the declared disjoint ARC1 all-training capacity, so do not fabricate another comparable joint all-training transfer cohort. P0042 is the committed metadata-only capacity/protocol decision: its mixed source partitions are non-comparable and may be used only as a new exposed development lane.
5. Use P0042's retained reports only to classify visible failure mechanisms, then preregister at most one bounded generic relation against its immutable same-cohort baseline. Do not promote `uniform_frame_size_fill` or a P0043 relation without a selected exact result on a separately frozen mixed-partition transfer cohort; P0041's zero frame-size selections remain negative evidence.
6. Add only generic, independently testable perception/action families. They must have a synthetic contract, ambiguity/tie refusal, and trace-level counterexample test; they must not branch on task ID, fixed colors, GT labels, or `_fit_algorithm_*` fitting.
7. Replace the recorded ARC3 replay with a blind public game-session adapter before making any ARC3-solving claim; use oracle assets only after play for V&V.

## Promotion rule

A primitive is reusable only after it has: a generic definition, deterministic unit and mutation checks, at least one task-independent transfer result, a complete diagram/receipt, and no test-target access in the live controller. Otherwise it remains experimental evidence, not a solver capability.
